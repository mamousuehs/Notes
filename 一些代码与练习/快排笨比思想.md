```cpp
#include<iostream>
#include<vector>
using namespace std;
const int N=1e6+10;
int n;
int q[N];
void quicksort(int q[],int l,int r){
    if (l>=r)return;
    int x=q[l];或者 int pivot_pos=l+(r-1)/2;//取中间值，当然取随机值也行
    vector<int> a,b;
    for (int i = l+1; i <= r; i++)
    {
        if (q[i]<=x)a.push_back(q[i]);
        else b.push_back(q[i]);
    }
    int k=l;
    for(int num:a)q[k++]=num;
    int mid=k;
    q[k++]=x;
    for(int num:b)q[k++]=num;
    quicksort(q,l,mid-1);
    quicksort(q,mid+1,r);
}
```
艾斯比，忘记怎么追加数组了，以后再写吧，等再去学一下[[vector]]还有释放内存[[delete]]
```CPP
for (int i = l+1; i < r; i++)
```
加一是因为我们取了`q[l]`作为基准值即第一个数，待会放回去的时候避免重复所以加一。如果取了随机值或者中间值，那就一样的要跳过那个值所在的位置。
```CPP
for (int i = l; i <= r; i++) { // 核心：跳过基准值的位置，避免重复加入a/b 
if (i == pivot_pos) continue;

```
### 2026/3/3，我发现不用追加数组，让`a[]`,`b[]`和`q[]`一样大然后按位放回去就行了
```cpp
#include<iostream>
#include<vector>
using namespace std;
const int N=1e6+10;
int n;
int q[N];
void quicksort(int q[],int l,int r){
    if (l>=r)return;
    int x=q[l];
    int a[N],b[N];
    for (int i = l; i < r; i++)
    {
        if (q[i]<=x)a.push_back(q[i]);
        else b.push_back(q[i]);
        
    }
}
```